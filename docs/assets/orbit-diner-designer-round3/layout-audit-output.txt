  - grid track: src/orbit.css — ".od-appliances" sets "grid-template-columns: 1fr 1.02fr"; a bare `1fr` is `minmax(auto, 1fr)`, whose automatic minimum is the min-content width of the track's contents — one non-shrinkable child pins the track open and the page scrolls horizontally. Write `minmax(0, 1fr)` (issue #457, audit F2).
  - grid track: src/orbit.css — ".od-shortcuts" sets "grid-template-columns: minmax(130px, 0.8fr) 1.2fr"; a bare `1.2fr` is `minmax(auto, 1.2fr)`, whose automatic minimum is the min-content width of the track's contents — one non-shrinkable child pins the track open and the page scrolls horizontally. Write `minmax(0, 1.2fr)` (issue #457, audit F2).
  - grid track: src/orbit.css — ".od-counter-papers" sets "grid-template-columns: 0.8fr 1.2fr"; a bare `0.8fr` is `minmax(auto, 0.8fr)`, whose automatic minimum is the min-content width of the track's contents — one non-shrinkable child pins the track open and the page scrolls horizontally. Write `minmax(0, 0.8fr)` (issue #457, audit F2).
  - grid track: src/orbit.css — ".od-appliances" sets "grid-template-columns: 1fr"; a bare `1fr` is `minmax(auto, 1fr)`, whose automatic minimum is the min-content width of the track's contents — one non-shrinkable child pins the track open and the page scrolls horizontally. Write `minmax(0, 1fr)` (issue #457, audit F2).
  - grid track: src/orbit.css — ".od-counter-papers" sets "grid-template-columns: 1fr"; a bare `1fr` is `minmax(auto, 1fr)`, whose automatic minimum is the min-content width of the track's contents — one non-shrinkable child pins the track open and the page scrolls horizontally. Write `minmax(0, 1fr)` (issue #457, audit F2).
  - grid track: src/orbit.css — ".od-results" sets "grid-template-columns: 1fr"; a bare `1fr` is `minmax(auto, 1fr)`, whose automatic minimum is the min-content width of the track's contents — one non-shrinkable child pins the track open and the page scrolls horizontally. Write `minmax(0, 1fr)` (issue #457, audit F2).
  - grid track: src/orbit.css — ".od-disposition-keys" sets "grid-template-columns: 1fr"; a bare `1fr` is `minmax(auto, 1fr)`, whose automatic minimum is the min-content width of the track's contents — one non-shrinkable child pins the track open and the page scrolls horizontally. Write `minmax(0, 1fr)` (issue #457, audit F2).
  - grid track: src/orbit.css — ".od-shortcuts" sets "grid-template-columns: 1fr"; a bare `1fr` is `minmax(auto, 1fr)`, whose automatic minimum is the min-content width of the track's contents — one non-shrinkable child pins the track open and the page scrolls horizontally. Write `minmax(0, 1fr)` (issue #457, audit F2).
  - grid track: src/orbit.css — ".od-appliances" sets "grid-template-columns: 1fr"; a bare `1fr` is `minmax(auto, 1fr)`, whose automatic minimum is the min-content width of the track's contents — one non-shrinkable child pins the track open and the page scrolls horizontally. Write `minmax(0, 1fr)` (issue #457, audit F2).
  - grid track: src/orbit.css — ".od-counter-papers" sets "grid-template-columns: 1fr"; a bare `1fr` is `minmax(auto, 1fr)`, whose automatic minimum is the min-content width of the track's contents — one non-shrinkable child pins the track open and the page scrolls horizontally. Write `minmax(0, 1fr)` (issue #457, audit F2).
  - grid track: src/orbit.css — ".od-ticket-column" sets "grid-template-columns: 1fr auto"; a bare `1fr` is `minmax(auto, 1fr)`, whose automatic minimum is the min-content width of the track's contents — one non-shrinkable child pins the track open and the page scrolls horizontally. Write `minmax(0, 1fr)` (issue #457, audit F2).
  - grid track: src/orbit.css — ".od-results" sets "grid-template-columns: 1fr"; a bare `1fr` is `minmax(auto, 1fr)`, whose automatic minimum is the min-content width of the track's contents — one non-shrinkable child pins the track open and the page scrolls horizontally. Write `minmax(0, 1fr)` (issue #457, audit F2).
  - font-size floor: src/orbit.css declares `font-size: clamp(18px, 2.1vw, 30px)` — "clamp(18px, 2.1vw, 30px)" is not an absolute size — use a --ct-text-* token. Relative units (em/%/smaller) compound with their context, so no source guard can prove they clear the floor (issue #600).
  - font-size floor: src/orbit.css declares `font-size: clamp(15px, 1.6vw, 24px)` — "clamp(15px, 1.6vw, 24px)" is not an absolute size — use a --ct-text-* token. Relative units (em/%/smaller) compound with their context, so no source guard can prove they clear the floor (issue #600).
  - font-size floor: src/orbit.css declares `font-size: clamp(25px, 3.5vw, 48px)` — "clamp(25px, 3.5vw, 48px)" is not an absolute size — use a --ct-text-* token. Relative units (em/%/smaller) compound with their context, so no source guard can prove they clear the floor (issue #600).
  - font-size floor: src/orbit.css declares `font-size: clamp(18px, 1.65vw, 26px)` — "clamp(18px, 1.65vw, 26px)" is not an absolute size — use a --ct-text-* token. Relative units (em/%/smaller) compound with their context, so no source guard can prove they clear the floor (issue #600).
  - font-size floor: src/orbit.css declares `font-size: clamp(14px, 4.4vw, 18px)` — "clamp(14px, 4.4vw, 18px)" is not an absolute size — use a --ct-text-* token. Relative units (em/%/smaller) compound with their context, so no source guard can prove they clear the floor (issue #600).
