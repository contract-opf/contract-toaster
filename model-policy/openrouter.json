{
  "_comment": [
    "Model-policy artifact for the DTS (Docker Compose) deployment target.",
    "",
    "The DTS deployment calls a direct model provider (OpenRouter, an",
    "OpenAI-compatible API) instead of AWS Bedrock. This file is the DTS",
    "counterpart of bedrock-us-east-1.json: it pins the model matrix and the",
    "per-token pricing used by the spend model. It is NOT scanned by the",
    "Bedrock-access CI gate (that gate targets bedrock-us-east-1.json only) —",
    "OpenRouter model IDs use the provider/model form (e.g. anthropic/...)",
    "and the single-region-native-inference residency check does not apply.",
    "",
    "There is no AWS regional pricing premium here: OpenRouter pricing is",
    "flat/all-in per token.",
    "",
    "Model matrix parity (issue #269): this file's primary/critic model_ids",
    "must agree with model-policy/bedrock-us-east-1.json on role structure",
    "and model FAMILY per role (Opus-class primary, Sonnet-class critic — see",
    "README.md's pinned matrix), allowing only provider-specific ID syntax to",
    "differ (e.g. \"anthropic.claude-opus-4-8\" vs",
    "\"anthropic/claude-opus-4.8\"). tests/lint-model-policy-consistency.py",
    "is the CI gate that enforces this.",
    "",
    "GENERATION may now differ in ONE direction (issue #604): this file may",
    "pin a NEWER generation of the same family than Bedrock, and only when the",
    "role entry declares `matrix_divergence_note` saying why. This file",
    "falling BEHIND Bedrock is still an unconditional gate failure — that was",
    "the #269 bug. models.primary carries such a declaration today: the DTS",
    "target ships Claude Opus 5 while the Bedrock artifact still pins Opus",
    "4.8, because a Bedrock repin additionally needs account model access, a",
    "re-measured on-demand quota, an IAM grant scoped to that exact",
    "foundation-model ARN in infra/lib/nested/pipeline-stack.ts, and a",
    "quarterly recertification run.",
    "",
    "SOURCE NOTE (issue #269, verified 2026-07-13 against",
    "https://openrouter.ai/anthropic/claude-opus-4.8 and",
    "https://openrouter.ai/anthropic/claude-sonnet-4.6): the",
    "cost_per_million_* rates below reflect OpenRouter's listing for",
    "Claude Opus 4.8 ($5.00 / $25.00 per million input/output tokens) and",
    "Claude Sonnet 4.6 ($3.00 / $15.00 per million input/output tokens).",
    "models.primary's rates are NOT from that pass: issue #604 repinned it to",
    "anthropic/claude-opus-5, whose $5.00 / $25.00 rates come from the",
    "2026-08-02 SELECTABLE SOURCE NOTE pass below for that same model_id.",
    "Re-verify against https://openrouter.ai/models before production use if",
    "this file has aged past the pinned matrix's last update.",
    "The model IDs may also be overridden per-deployment via the env vars",
    "OPENROUTER_PRIMARY_MODEL_ID / OPENROUTER_CRITIC_MODEL_ID — an override",
    "is honored by OpenRouterModelClient.invoke() but logged as an explicit",
    "override (backend/src/model_client.py::enforce_openrouter_policy_model_id).",
    "",
    "`selectable` (issue #445) is the ALLOWLIST an admin may pick from in the",
    "app's \"Model & API key\" tab, stored in the model-settings table and",
    "resolved by backend/src/model_settings.py. It exists so a deliberate",
    "admin choice is possible WITHOUT weakening the runtime invariant that",
    "enforce_openrouter_policy_model_id exists for: an arbitrary, unlisted",
    "model is still refused before a request is spent on it. Resolution",
    "precedence for an effective model id is admin selection > env override >",
    "the models.primary/models.critic default pinned above.",
    "",
    "`selectable` is deliberately NOT covered by",
    "tests/lint-model-policy-consistency.py: that gate enforces Opus/Sonnet",
    "family+generation parity with bedrock-us-east-1.json, which only the",
    "DEFAULT pins are meant to satisfy. A non-Anthropic selectable entry",
    "(Gemini, GPT, Kimi, DeepSeek) has no opus/sonnet/haiku family token by",
    "design, and the Bedrock target has no admin-selection concept at all.",
    "",
    "`tier` is CONSIDERED JUDGEMENT, not a benchmark result — the UI must",
    "label it as guidance, never as a measurement. The cost rates ARE",
    "computed facts and may be stated flatly.",
    "",
    "SELECTABLE SOURCE NOTE (issue #445, verified 2026-08-02 against",
    "https://openrouter.ai/api/v1/models and .../models?zdr=true): SIX",
    "`selectable` entries below (opus-5, gpt-5.6-sol, sonnet-5,",
    "gemini-3.1-pro-preview, kimi-k3, deepseek-v4-pro) were present in the",
    "live catalogue, listed a ZDR endpoint, and advertised",
    "`response_format`/`structured_outputs` (non-negotiable — this app",
    "requires strict JSON) plus `reasoning_effort`. Re-verify the rates",
    "against that endpoint before landing a change here. The one other",
    "`selectable` entry -- anthropic/claude-sonnet-4.6, added by issue #589",
    "-- is NOT covered by this 2026-08-02 pass: see DEFAULTS-ARE-SELECTABLE",
    "below for what is and is not verified for it (no ZDR-endpoint or",
    "structured-outputs confirmation exists for it in this file). Note that",
    "opus-5 is one of",
    "the SIX and is ALSO models.primary since issue #604 — it was verified",
    "here as a selectable id, and the pin inherits exactly that verification",
    "because it is the same model_id.",
    "",
    "DEFAULTS-ARE-SELECTABLE (issue #589): models.primary's and",
    "models.critic's model_id (anthropic/claude-opus-5,",
    "anthropic/claude-sonnet-4.6) are ALSO listed as `selectable` entries",
    "below. Before that fix they were not, so an admin who ever chose",
    "anything from the picker had no dropdown entry leading back to the only",
    "pair with verified working live reviews behind it (2026-08-21 prod",
    "incident: recovery required a null/null write, not a UI selection).",
    "anthropic/claude-opus-4.8 was REMOVED from `selectable` on 2026-08-23 by",
    "OWNER DECISION, after issue #604 moved the primary pin off it. Read this",
    "before restoring it or citing #589 at it: the removal knowingly gives up",
    "#589's recovery route. 4.8 was the primary half of the only pairing with",
    "verified live reviews behind it, and there is now no dropdown entry",
    "leading back to that pairing. If an incident makes opus-5 unusable,",
    "recovery is a null/null write or a policy edit + deploy, NOT a UI",
    "selection -- which is precisely the 2026-08-21 situation #589 was added",
    "to prevent. The trade-off was put to the owner explicitly, with #589's",
    "provenance, and removal was chosen anyway. Do not silently re-add it;",
    "if the risk is reconsidered, that is a new decision to record here.",
    "models.critic does not declare `structured_outputs`, so its own",
    "`selectable` twin deliberately omits it too --",
    "backend/src/model_client.py::openrouter_model_capabilities resolves",
    "capabilities from the role pin first, and the live evidence for Sonnet",
    "4.6 is that its valid strict JSON comes from prompting, not from a",
    "verified provider-native structured-output capability. models.primary is",
    "different since #604: opus-5 WAS verified to advertise structured",
    "outputs (SELECTABLE SOURCE NOTE), so the pin declares it, matching its",
    "own `selectable` entry rather than silently failing the default closed.",
    "tests/lint-model-policy-consistency.py's check_defaults_are_selectable",
    "enforces this membership as a CI gate.",
    "",
    "REASONING BUDGET (issue #527): `reasoning_max_tokens` on models.primary,",
    "models.critic, and each `selectable` entry is the token allowance",
    "backend/src/model_client.py::OpenRouterModelClient.invoke ADDS on top of",
    "the pass's own content budget when it sends `max_tokens` — never carved",
    "out of it. OpenRouter's `max_tokens` is a COMBINED ceiling across a",
    "reasoning-class model's `reasoning` AND `content` tokens; a model given",
    "no allowance can silently spend the whole budget on reasoning and return",
    "empty content. An operator-run live probe (issue #527, 2026-08-03)",
    "against real structured-output requests found exactly two of the eight",
    "model_id values below reasoning-class — moonshotai/kimi-k3 and",
    "google/gemini-3.1-pro-preview, both of which return `stop` with valid",
    "JSON once given a several-thousand-token allowance; every other entry",
    "returned 0 reasoning tokens at both a tight and a roomy budget. `0` is",
    "the default for every model this file does not name an allowance for,",
    "which keeps every currently-passing prompt fixture byte-identical.",
    "",
    "CAPABILITY DESCRIPTOR (issue #562): optional per-model",
    "`structured_outputs` / `prompt_caching` fields, read by",
    "backend/src/model_client.py::openrouter_model_capabilities. Absent means",
    "False -- fail-closed, never an assumed capability this file did not",
    "actually declare. `structured_outputs: true` below on every `selectable`",
    "entry EXCEPT anthropic/claude-sonnet-4.6",
    "(which declares nothing -- see DEFAULTS-ARE-SELECTABLE) is exactly the",
    "SELECTABLE SOURCE NOTE verification above (each of those six ids",
    "advertised `response_format`/`structured_outputs`) turned into a field a",
    "caller can read instead of only prose. `models.critic` declares neither",
    "field: that pricing verification (2026-07-13) predates the capability",
    "check and never confirmed it, so this file has nothing verified to",
    "assert for anthropic/claude-sonnet-4.6 yet -- left absent rather than",
    "guessed. `models.primary` DOES declare `structured_outputs: true` since",
    "issue #604 repinned it to anthropic/claude-opus-5, an id the 2026-08-02",
    "pass did verify; without it the role loop would resolve the shipped",
    "default to False and silently drop `output_schema` from every default",
    "review. `prompt_caching` is left absent everywhere in this file: no",
    "verification pass has confirmed it for any OpenRouter-routed id.",
    "",
    "PREFLIGHT ROLE (issue #491): `models.preflight` pins the cheap, fast",
    "classifier the upload-time preflight check (backend/src/review_routes.py's",
    "POST /api/reviews/preflight) uses to guess agreement type + paper side.",
    "Deliberately its OWN role, not the primary/critic admin-selection ladder",
    "(backend/src/model_settings.py) -- an admin picking a stronger primary/critic",
    "model must never silently repoint this advisory, every-upload check onto",
    "something expensive. Pinned to the Budget tier already in `selectable` below",
    "(DeepSeek V4 Pro) rather than duplicated as a second selectable entry.",
    "backend/src/model_client.py::openrouter_preflight_model_id resolves it",
    "(OPENROUTER_PREFLIGHT_MODEL_ID env override, then this pin);",
    "enforce_openrouter_policy_model_id accepts the pin the same way it accepts",
    "the primary/critic pins. tests/lint-model-policy-consistency.py's",
    "SHARED_ROLES tuple deliberately does not include \"preflight\" -- there is no",
    "Bedrock counterpart to keep in lockstep (the AWS/Bedrock target has no",
    "preflight concept yet); this role exists only on the OpenRouter/DTS artifact.",
    "",
    "COVER NOTE ROLE (issue #499): `models.cover_note` pins the model",
    "\"Butter it\" (POST /api/reviews/{review_id}/cover-note) uses to draft the",
    "counterparty cover email from a FINISHED review's own analysis artifact",
    "(issues[] + verdict_summary already on the reviews row -- never the raw",
    "document, never a re-run of review). Deliberately its OWN role, same",
    "reasoning as PREFLIGHT ROLE above: this is prose summarization of an",
    "already-decided review, not legal judgment, so an admin's primary/critic",
    "selection must never silently repoint it onto something stronger (and",
    "pricier) than it needs. Pinned to anthropic/claude-sonnet-5 -- a",
    "cheap-to-mid, non-reasoning (`reasoning_max_tokens: 0`) model distinct",
    "from every other pinned role's model_id (primary=opus-5,",
    "critic=sonnet-4.6, preflight=deepseek-v4-pro): issue #491 fix round 1's",
    "lesson (a pin that happens to also be a `selectable` entry must still be",
    "resolved and capability-checked through its OWN role, never only via",
    "incidental selectable-set membership) applies here too, so",
    "openrouter_model_capabilities' role loop and enforce_openrouter_policy_",
    "model_id both name \"cover_note\" explicitly rather than relying on this",
    "model_id also being selectable. backend/src/model_client.py::",
    "openrouter_cover_note_model_id resolves it (OPENROUTER_COVER_NOTE_MODEL_ID",
    "env override, then this pin), NOT run through the admin-selection ladder",
    "(_resolved_admin_model_id) -- same non-admin-selectable posture as",
    "preflight. tests/lint-model-policy-consistency.py's SHARED_ROLES tuple",
    "deliberately does not include \"cover_note\" either, for the same reason:",
    "no Bedrock counterpart exists for this role."
  ],
  "schema_version": "1",
  "provider": "openrouter",
  "base_url": "https://openrouter.ai/api/v1",
  "models": {
    "primary": {
      "role": "primary_reviewer",
      "model_id": "anthropic/claude-opus-5",
      "matrix_divergence_note": "Issue #604: the DTS/OpenRouter target's primary reviewer is pinned one generation AHEAD of bedrock-us-east-1.json (anthropic.claude-opus-4-8). Owner decision -- Opus 5 has been listed in `selectable` and reachable on OpenRouter since 2026-08-02, so shipping 4.8 as the default was leaving the better model on the shelf. The Bedrock pin deliberately did NOT move with it: that id additionally needs account model access, a re-measured on-demand quota (granted_tpm/granted_rpm and the review_throughput_ceiling / max_eval_parallelism derived from them), an IAM grant scoped to the exact foundation-model ARN in infra/lib/nested/pipeline-stack.ts, and a quarterly recertification run -- none of which this change did. tests/lint-model-policy-consistency.py reads this field; deleting it makes the gate fail.",
      "approx_tokens_per_review_input": 60000,
      "approx_tokens_per_review_output": 8000,
      "cost_per_million_input_usd": 5.00,
      "cost_per_million_output_usd": 25.00,
      "reasoning_max_tokens": 0,
      "structured_outputs": true,
      "request_contract": {
        "omit_sampling_params": ["temperature", "top_p", "top_k"]
      }
    },
    "critic": {
      "role": "adversarial_critic",
      "model_id": "anthropic/claude-sonnet-4.6",
      "approx_tokens_per_review_input": 70000,
      "approx_tokens_per_review_output": 5000,
      "cost_per_million_input_usd": 3.00,
      "cost_per_million_output_usd": 15.00,
      "reasoning_max_tokens": 0,
      "request_contract": {
        "omit_sampling_params": ["temperature", "top_p", "top_k"]
      }
    },
    "preflight": {
      "role": "preflight_classifier",
      "model_id": "deepseek/deepseek-v4-pro",
      "approx_tokens_per_review_input": 3000,
      "approx_tokens_per_review_output": 200,
      "cost_per_million_input_usd": 0.435,
      "cost_per_million_output_usd": 0.87,
      "reasoning_max_tokens": 0,
      "structured_outputs": true,
      "request_contract": {
        "omit_sampling_params": ["temperature", "top_p", "top_k"]
      }
    },
    "cover_note": {
      "role": "cover_note_drafter",
      "model_id": "anthropic/claude-sonnet-5",
      "approx_tokens_per_review_input": 4000,
      "approx_tokens_per_review_output": 300,
      "cost_per_million_input_usd": 2.00,
      "cost_per_million_output_usd": 10.00,
      "reasoning_max_tokens": 0,
      "structured_outputs": true,
      "request_contract": {
        "omit_sampling_params": ["temperature", "top_p", "top_k"]
      }
    }
  },
  "selectable": [
    {
      "model_id": "anthropic/claude-sonnet-4.6",
      "display_name": "Claude Sonnet 4.6",
      "tier": "High",
      "note": "The policy default for the critic pass.",
      "cost_per_million_input_usd": 3.00,
      "cost_per_million_output_usd": 15.00,
      "reasoning_max_tokens": 0
    },
    {
      "model_id": "anthropic/claude-opus-5",
      "display_name": "Claude Opus 5",
      "tier": "Highest",
      "note": "Strongest nuanced legal reasoning.",
      "cost_per_million_input_usd": 5.00,
      "cost_per_million_output_usd": 25.00,
      "context_length": 1000000,
      "reasoning_max_tokens": 0,
      "structured_outputs": true
    },
    {
      "model_id": "openai/gpt-5.6-sol",
      "display_name": "GPT-5.6 Sol",
      "tier": "Highest",
      "note": "Frontier model from a different lab — a genuine second opinion.",
      "cost_per_million_input_usd": 5.00,
      "cost_per_million_output_usd": 30.00,
      "context_length": 1050000,
      "reasoning_max_tokens": 0,
      "structured_outputs": true
    },
    {
      "model_id": "anthropic/claude-sonnet-5",
      "display_name": "Claude Sonnet 5",
      "tier": "High",
      "note": "Near-frontier at roughly 40% of Opus.",
      "cost_per_million_input_usd": 2.00,
      "cost_per_million_output_usd": 10.00,
      "context_length": 1000000,
      "reasoning_max_tokens": 0,
      "structured_outputs": true
    },
    {
      "model_id": "google/gemini-3.1-pro-preview",
      "display_name": "Gemini 3.1 Pro (preview)",
      "tier": "High",
      "note": "Very strong over long context.",
      "cost_per_million_input_usd": 2.00,
      "cost_per_million_output_usd": 12.00,
      "context_length": 1048576,
      "reasoning_max_tokens": 4000,
      "structured_outputs": true
    },
    {
      "model_id": "moonshotai/kimi-k3",
      "display_name": "Kimi K3",
      "tier": "Good",
      "note": "Strongest open-weight model.",
      "cost_per_million_input_usd": 3.00,
      "cost_per_million_output_usd": 15.00,
      "context_length": 1048576,
      "reasoning_max_tokens": 4000,
      "structured_outputs": true
    },
    {
      "model_id": "deepseek/deepseek-v4-pro",
      "display_name": "DeepSeek V4 Pro",
      "tier": "Budget",
      "note": "About 15x cheaper than the highest tier.",
      "cost_per_million_input_usd": 0.435,
      "cost_per_million_output_usd": 0.87,
      "context_length": 1048576,
      "reasoning_max_tokens": 0,
      "structured_outputs": true
    }
  ]
}
