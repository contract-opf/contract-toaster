# Secrets — never commit
.env
.env.*
!.env.example
*.pem
*.key
aws-credentials
credentials.json
client_secret*.json

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
# Python virtualenv lib dirs — anchored to repo root so infra/lib/ is not excluded
/lib/
/lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
.venv/
venv/
env/
ENV/

# Pytest, coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
.cache/
nosetests.xml
coverage.xml
*.cover
.hypothesis/

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# CDK
cdk.out/
# Real per-tenant deploy parameters (githubOwner, alarmsEmail, appDomain,
# adminEmail, hostedDomain, ...) live here for a real deployment — see
# infra/cdk.context.example.json and infra/README.md "Deploy identity
# context" (issue #409). CDK auto-creates this file (starting empty, {})
# on first synth; the structural, brand-free AZ-lookup cache CDK also needs
# for an offline synth lives in the committed infra/cdk.json instead, so
# this file never needs to be committed for tests/CI to pass.
cdk.context.json
cdk.context.json.bak
.cdk.staging/

# Vite / React build
dist/
dist-ssr/
*.local

# IDE
.vscode/
!.vscode/settings.json.example
.idea/
*.swp
*.swo
*~
.project
.settings/

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# Claude Code local-only config and worktrees (never commit local settings or agent worktrees)
.claude/settings.local.json
.claude/worktrees/

# AWS
.aws-sam/
samconfig.toml

# Logs
*.log
logs/

# Local-only test inputs / outputs (in case anyone drops counterparty docs in)
local-uploads/
local-outputs/
scratch/
tmp/

# Submodule build artifacts that shouldn't ship up
backend/vendor/*/build/
backend/vendor/*/dist/
backend/vendor/*/__pycache__/

# Tenant production overlay secrets (never committed)
overlay/.env
overlay/*.secret
